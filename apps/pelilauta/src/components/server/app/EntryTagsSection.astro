---
export interface Props {
  tags: string[];
}
const { tags } = Astro.props;
---
{tags && tags.length > 0 &&
  <div class="chip-list mt-2">
    {tags.map((tag) => (
      <a href={`/tags/${tag}`} class="chip">
        {tag}
      </a>
    ))}
  </div>
}
