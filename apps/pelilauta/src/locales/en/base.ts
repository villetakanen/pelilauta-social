import type { Locale } from '../../utils/i18n';

/** What the base application's own chrome says. */
export const base: Locale = {
  rail: {
    label: 'Main menu',
    home: 'Home',
    channels: 'Discussions',
    sites: 'Games',
  },
};
