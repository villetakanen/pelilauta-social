---
import Admin from '@layouts/Admin.astro';
import { requireAdmin } from '@pelilauta/admin/utils/requireAdmin';
import ChannelsAdmin from '../../components/svelte/admin/channels/ChannelsAdmin.svelte';

const denied = await requireAdmin(Astro);
if (denied) return denied;
---
<Admin title="Forum Administration" description="Comprehensive forum channel management tools">
  <ChannelsAdmin client:only="svelte" />
</Admin>
