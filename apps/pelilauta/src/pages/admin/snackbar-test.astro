---
import Admin from '@layouts/Admin.astro';
import { requireDeveloperTools } from '@pelilauta/admin/utils/requireDeveloperTools';
import SnackbarTestApp from '../../components/svelte/admin/SnackbarTestApp.svelte';

const denied = await requireDeveloperTools(Astro);
if (denied) return denied;
---
<Admin 
  title="Snackbar Test Utility" 
  description="Test snackbar notifications">
  <SnackbarTestApp client:only="svelte" />
</Admin>
