# design.pelilauta.social is a separate Netlify site from pelilauta.social.
[build]
  base = "."
  command = "pnpm --filter design build"
  publish = "apps/design/dist"

[build.environment]
  NODE_VERSION = "22"
