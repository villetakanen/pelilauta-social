---
/**
 * ColorRamp — one family's tonal scale, as colour.
 *
 * v20's colour book renders every family this way. Steps are read from the
 * stylesheet, so a ramp cannot show a step that no longer exists.
 *
 * Reads styles/chroma.css: the literal layer the palette ends in (ADR 0002).
 *
 * Book: apps/design/src/content/principles/color-system.mdx
 */
import { parseTokens } from './tokenTable';

interface Props {
  /** chroma.css, imported by the book with `?raw`. */
  source: string;
  /** Family name, e.g. `surface` — matches `--chroma-<family>-<step>`. */
  family: string;
  /** What the family is for. One line, shown beside the name. */
  intent?: string;
}

const { source, family, intent } = Astro.props;

const steps = parseTokens(source, { prefix: `--chroma-${family}-` }).map(
  (token) => ({
    step: token.name.replace(`--chroma-${family}-`, ''),
    name: token.name,
  }),
);

if (steps.length === 0) {
  throw new Error(
    `ColorRamp found no steps for family "${family}". The palette no longer declares it.`,
  );
}
---

<figure class="ramp">
  <figcaption>
    <strong>{family}</strong>{intent && <span> — {intent}</span>}
  </figcaption>
  <ol>
    {steps.map(({ step, name }) => (
      <li>
        <span class="swatch" style={`background: var(${name});`} title={name}></span>
        <span class="step">{step}</span>
      </li>
    ))}
  </ol>
</figure>

<style>
  .ramp ol {
    display: flex;
    flex-wrap: wrap;
    gap: 2px;
    margin: 0;
    padding: 0;
    list-style: none;
  }
  .ramp li {
    flex: 1 1 3rem;
    text-align: center;
  }
  .swatch {
    display: block;
    block-size: 3rem;
    border: 1px solid var(--cn-border);
  }
  .step {
    display: block;
    font-size: 0.75rem;
    font-variant-numeric: tabular-nums;
  }
  figcaption {
    margin-block-end: 0.25rem;
  }
</style>
