---
title: The book's title
description: One sentence. It is the navigation subtitle and the page's meta description, so write it for someone deciding whether to open the page.
order: 1
prose: true
---

{/*
  BASE BOOK — copy to apps/design/src/content/base/<slug>.mdx, delete these comments.
  Rules are the design-system-book skill's. Link books, never a spec.
*/}

One or two sentences: what ships, and how an application gets it.

## A heading naming what this defines

<SomeSpecimen />

Values go in a table. Prose only where a table cannot carry it, and only for what
the reader must do — never for why the rule exists.

| Name | Value | Applies to |
| :--- | :--- | :--- |
| `--cn-grid` | `0.5rem` | The atomic unit of alignment. |
